services:
  - type: web
    name: tp3-back
    runtime: node
    rootDir: TP3
    plan: free
    buildCommand: npm install
    startCommand: node index.js

  - type: web
    name: tp1-front
    runtime: static
    rootDir: TP1
    buildCommand: ""
    staticPublishPath: .